apiVersion: apps/v1
kind: Deployment
metadata:
  name: openclaw-manager
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openclaw-manager
  template:
    metadata:
      labels:
        app.kubernetes.io/name: openclaw-manager
    spec:
      serviceAccountName: openclaw-manager
      tolerations:
        - key: kubernetes.io/arch
          operator: Equal
          value: amd64
          effect: NoSchedule
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: us-central1-docker.pkg.dev/learneroid/openclaw/openclaw-manager:latest
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: PORT
              value: "3000"
            - name: OPENCLAW_NAMESPACE
              value: openclaw
            - name: OPENCLAW_IMAGE_REPO
              value: us-central1-docker.pkg.dev/learneroid/openclaw/openclaw-gke
            # 중요: latest 사용 금지. imagePullPolicy: IfNotPresent이므로
            # latest를 재사용하면 노드 캐시로 인해 이전 이미지가 사용됩니다.
            # 반드시 git SHA 등 고유 태그를 사용하세요.
            - name: OPENCLAW_IMAGE_TAG
              value: "27b5d60f3"
            - name: OPENCLAW_DOMAIN_BASE
              value: openclaw.zazz.buzz
            - name: OPENCLAW_MANAGER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openclaw-manager
                  key: API_KEY
            # Ingress 활성화: 각 인스턴스에 {userId}.openclaw.zazz.buzz 도메인 + ManagedCertificate 생성
            - name: OPENCLAW_INGRESS_ENABLED
              value: "true"
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - NET_RAW
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: openclaw-manager
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw-manager
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: openclaw-manager
